server:
  port: 8081

spring:
  application:
    name: auth-service
  profiles:
    default: prod

jwt:
  secret: ${JWT_SECRET:easystay-secret-key-2026-microservices-architecture-secure}
  expiration: 86400000

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
