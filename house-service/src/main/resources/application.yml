server:
  port: 8082

spring:
  application:
    name: house-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/housedb}
    username: ${SPRING_DATASOURCE_USERNAME:houseuser}
    password: ${SPRING_DATASOURCE_PASSWORD:housepass}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

jwt:
  secret: ${JWT_SECRET:easystay-secret-key-2026-microservices-architecture-secure}

auth:
  service:
    url: ${AUTH_SERVICE_URL:http://localhost:8081}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
